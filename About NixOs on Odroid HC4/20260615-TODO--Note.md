---
title:        TODO
subtitle:     Note
author:       Jgetreu
date:         2026-06-15
lang:         en-US
---

1. ~~Get the USB keyboard working.~~ [done: usb-rebind service; verified 2026-06-16]
2. Find a Copilot alternative.
3. ~~Decrypt and mount the disks.~~ [done: mdadm + LUKS passphrase verified 2026-06-16; passphrase in Raid encryption recovery note]
4. ~~Extend the software in the image (e.g. Unison).~~ [done: mdadm, cryptsetup, e2fsprogs, mc in base image; unison in NAS config via nixos-rebuild (OCaml cannot cross-compile, but builds natively on aarch64)]
5. Configure NAS services (RAID auto-mount at boot, file sharing).
   - ~~NFS server~~ [done 2026-06-17: NFS configured and working; firewall ports 111/2049/20048 opened; NAS config has full user/group setup (getreu/jens/getreu-dev with matching primary groups)]
   - ~~Manual disk management~~ [done 2026-06-17: `start-disks` (assemble RAID1 → unlock LUKS → mount → start NFS) and `stop-disks` (stop NFS → unmount → close LUKS; asserts key wiped); remote unlock: `echo <passphrase> | ssh root@bucketnas3.lan nohup start-disks &`]
   - RAID auto-mount at boot [pending — currently requires manual `start-disks` after each boot]
